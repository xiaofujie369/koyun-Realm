# Koyun Relay V1 设计约束

## 产品目标

V1 只证明一件事：一条网页创建的 TCP 规则可以被指定 Agent 可靠接收并由 Realm 执行。所有设计优先保证闭环可测、失败可见和配置可回滚。

## 数据模型

- `nodes`：入口节点、Token 摘要、目标配置版本、已应用版本、心跳和错误；
- `rules`：节点所属的 TCP 监听与目标；同一节点的监听地址和端口唯一；
- `events`：最近 500 条控制面事件。

每次规则增删或启停都会在同一数据库事务内递增节点的 `desired_version`。Agent 成功应用后上报 `applied_version`。两者相同才代表同步完成。

## Agent 应用事务

1. HTTPS 拉取期望配置；
2. 本地校验地址、端口和重复监听；
3. 写入 `realm.candidate.toml`；
4. 停止旧 Realm；
5. 使用候选配置启动新 Realm并观察 1.5 秒；
6. 成功后原子替换 `realm.toml` 并保存版本；
7. 失败则使用内存中的旧规则重建配置并恢复旧 Realm；
8. 上报健康状态或回滚错误。

## 一键注册流程

1. 管理员创建节点，控制端只生成一个 30 分钟有效的 `kye_` 注册凭证；
2. 面板展示同时兼容 `curl` 和 `wget` 的单行安装命令；
3. VPS 获取安装脚本时，控制端生成永久 `kya_` Agent Token；
4. 安装脚本优先从控制端下载 Agent 包，GitHub 仅作为备用源；
5. 安装器识别发行版、包管理器与 CPU 架构，安装 Docker、Compose 和 Realm；
6. Agent 首次成功心跳后，控制端删除包含临时明文 Token 的注册记录；
7. 重新生成命令不会立即影响在线 Agent，只有执行新命令后才轮换凭据。

V1 更新会造成短暂连接中断。通用 TCP 无法在两个 Realm 进程间无损迁移既有连接；V2 将通过入口层连接排空或可复用监听器解决更新中断。

## 威胁边界

- 管理员接口使用 HttpOnly、SameSite Cookie；
- 修改接口执行同源检查；
- 登录连续失败 5 次后按来源地址封锁 5 分钟；
- Agent 使用 256 位随机 Token，数据库只保存摘要；
- 永久 Token 不显示在面板或 Shell 历史中；注册 URL 高熵、短时有效且禁止缓存；
- Agent 默认拒绝连接远程明文 HTTP 控制端；
- 规则字段严格限制长度、字符和端口范围；
- Realm 通过参数数组启动，不经过 shell，规则内容不能形成命令注入。

## 已知限制

- V1 每个入口节点只运行一个 Realm 进程；
- 新配置需要重启 Realm，既有连接会中断；
- Realm 模式暂不提供逐规则流量数据；
- 控制端为单实例 SQLite，不支持多副本同时写入；
- 未实现节点 Token 在线轮换；
- 未实现 Agent 自动升级或 Realm 自动下载。

这些限制是主动收缩范围，不是最终架构限制。
